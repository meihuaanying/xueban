# M3 学情闭环核心 API · 进度检查点（2026-09-15）

> **状态更新**：M3 已完成并通过出口门禁（promptfoo 按 B-005 记录），
> 正式验收证据见 `docs/verification/M3_2026-09-15.md` 与 `M3_2026-09-15.logs.txt`。
> 本文件保留为跨轮次推进的过程记录。

## 已完成任务

| 任务 | 交付 | 验收对照 | 实测 |
|------|------|----------|------|
| T3.1 BKT 学情画像（F-03） | `mastery_service`（BKT 更新/滑窗衰减/红黄绿分级）+ `GET /v1/profile/mastery` | 单测覆盖先验/衰减/边界；空数据引导态 | 18 用例；服务覆盖 98% |
| T3.2 自适应诊断（F-01/F-02） | CAT 选题（最弱知识点 + 难度贴近）、答对加难/答错降难、报告生成；`POST /v1/diagnosis/start|answer`、`GET /v1/diagnosis/{id}/report`；`POST /v1/mistakes/{id}/attribute`（LLM 归因五类枚举） | 难度调整分支单测；完整诊断 E2E；归因枚举严格校验 | 30 用例；diagnosis 86%、attribution 97% |
| T3.3 规划引擎（F-06~F-10） | 学习路径（前置→当前→拔高，含拓扑序与归档重排）、每日任务卡 + 打卡/连续天数（跨天边界）、考期倒排（三阶段/落后压缩/7 天警示）、前置回溯（连续错 3 题）、动态难度（升/降/保持阈值）、arq 定时重排 Worker | 路径拓扑单测；任务打卡 E2E；压缩分支；连续天数 23:59→00:01 单测；难度三分支与边界 | 35 用例；planner 90%、practice 100% |
| T3.4 守护型讲解编排（F-11 红线） | 三层提示状态机（0→1 思路→2 关键步骤→3 全解，禁止跳层）、求助层级落库、SSE 流式（`/v1/tutor/{id}/hint/stream`）、`solo_locked` 服务端锁定（F-28 预留）、分层提示词（第 1 层禁答红线） | 状态机跳层/回退/耗尽分支；三层全流程 E2E；SSE start/delta/done 事件；历史提示防重复；promptfoo 评测集就绪（50 条用例 + 提示词快照 + 红线断言） | 20 用例；tutor_service 98% |
| T3.5 讲解扩展（F-12~F-14、F-16 后端） | 多解法对比 + SymPy 等价性抽检（`answer_verified`）、生活化类比（analogy/mapping/caveat）、变式题生成入库（同知识点不同情境）+ 答错回炉（错题本 + 新讲解会话）、微课异步生成（600~1000 字讲解稿 + TTS 音频入 MinIO + 预签名直链，失败重试 ≤2 次后明示失败） | SymPy 等价/文本回退单测；多解法抽检计数；变式入库与归属校验；答对入画像、答错回炉链路；微课字数越界→FAILED(重试 3 次)；音频 RIFF 实测可下载 | 32 用例；tutor_ext 91%、lesson_service 92%、math_verify 100% |
| T3.6 出题与练习 + FSRS（F-17~F-19） | 智能出题（薄弱知识点占比 ≥60%、近 7 天去重、支持知识点定向）、练习作答闭环（BKT 画像 + 错题自动归集 + 动态难度回传 + FSRS 联动）、错题本（错因分组统计/筛选/一键重练/答对自动移出/PDF 导出含解析，内置 OFL 中文字体）、FSRS 调度（评分 1–4：Again 10 分钟重来、Good/Hard/Easy 间隔系数）、到期卡片进入今日任务卡 | 薄弱占比与去重断言；答错→入本→重练→移出 E2E；PDF 中文内容可抽取（%PDF + pypdf）；评分 1–4 间隔表/状态迁移/lapses 计数；越权与隔离 | 31 用例；fsrs 96%、practice 92%、mistake_book 90% |

## 当前仓库状态（实时）

- 测试：**325 passed**，总覆盖率 **91.13%**（门禁阈值 80%）
- 静态检查：`ruff check .` 0 错误；`mypy --strict app` 0 错误（79 源文件）
- 迁移：4 条（初始 schema / documents+向量 / micro_lessons / practice_records），全部往返无损
- 新增依赖：`arq`、`sympy`、`fpdf2`（PDF 导出运行时）；字体资产 `app/assets/fonts/ZCOOLXiaoWei-Regular.ttf`（OFL-1.1，附 LICENSE.txt）
- 评测资产：`evals/tutor.yaml`（红线 llm-rubric + 答案泄露 not-contains + 篇幅断言）、`evals/tutor_cases.json`（50 条）
- 相关提交产物：`app/services/{mastery,diagnosis,attribution,planner,practice,tutor,tutor_ext,lesson,fsrs,mistake_book}_service.py`、`app/services/{math_verify,tts,llm_json}.py`、`app/worker.py`、路由与 schema 若干

## 关键设计备注

1. 诊断复用 `exams/exam_answers` 表（kind=diagnosis），状态与选题队列存 `exam.meta`，无需新迁移。
2. 选题策略为「最弱知识点优先，其次难度最接近」，与 BKT 掌握度直接联动。
3. 归因提示词集中管理于 `prompts.py`（含版本号），T3.4 起 promptfoo 评测将引用同一文件。
4. 无题库/无学情数据时路径给出兜底（按教学顺序取前 N 个知识点），保证首用户体验不落空。

## 待办（M3 剩余）

- T3.7 批改（F-22~F-24 + SymPy 校验；`math_verify` 已就绪）
- T3.8 模考与无辅助测评（F-20/F-28，服务端强制禁提示 → 复用 `solo_locked`）
- T3.9 复盘：周报/日历/冲刺包（F-27/F-29/F-30）
- T3.10 埋点与行为画像（F-05 后端）
- promptfoo 实跑（待 DeepSeek Key，见 BLOCKERS B-005；配置与 50 条用例已就绪）
- FSRS 参考实现逐值对齐（见 BLOCKERS B-006，T10 评测阶段）
- M3 出口门禁全量执行 + `docs/verification/M3_<日期>.md` 证据
